language: php

php:
  - "7.0"

build:
  ci:
    - mkdir -p shippable/testresults
    - mkdir -p shippable/codecoverage
    - phpunit --configuration="phpunit.xml"
    - phpunit  --log-junit shippable/testresults/junit.xml --coverage-clover shippable/codecoverage.xml tests/